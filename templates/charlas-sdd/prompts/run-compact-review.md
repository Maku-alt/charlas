# Prompt: Run Compact Review

Ejecuta únicamente la fase `review` en modo `compact` con el rol `agents/review-charlas.md`.

El `Execution Package` debe identificar este prompt, el modo `compact` y el `Review Package` aplicable.

No modifiques el deck, el source, el candidato ni la evidencia de build; no avances automáticamente a otra fase.
